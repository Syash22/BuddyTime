<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuddyTIme"
        tools:targetApi="31">

        <activity
            android:name=".LoginSignIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".AddSchedule"></activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".LoginSignUp"/>
        <activity android:name=".DetailSchedule"/>
        <activity android:name=".SummarySchedule" android:theme="@android:style/Theme.Holo.Dialog"></activity>
        <activity android:name=".AcceptFriend"/>
        <activity android:name=".RequestFriend"/>
        <activity android:name=".CalendarMain"/>
        <activity android:name=".FriendMain"/>
        <activity android:name=".editSchedule"/>
        <meta-data android:name="android.max_aspect" android:value="2.1" />
    </application>

</manifest>